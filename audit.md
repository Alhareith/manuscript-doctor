# تقرير تدقيق المشروع المرفق

> **تنبيه زمني:** هذا الملف هو سجل تدقيق وتحليل سابق لمرحلة إعادة البناء. العبارات المستقبلية مثل «ستضاف» و«سيتم» تصف قرارات كانت مقترحة وقت كتابة التقرير، ولا تمثل وحدها حالة التطبيق الحالية. المرجع الحالي للتنفيذ هو `docs/current-baseline.md`، أما الأدلة الأخيرة فتوجد في `manual-qa-report.md` و`UNIFIED_RESULTS_UPDATE_AR.md`.

## خلاصة تنفيذية
المشروع الحالي هو تطبيق Flask لمعالجة صور المخطوطات، وليس مجرد نموذج واجهة. قوته الأساسية في المنطق الأكاديمي المتعلق بـ **التشخيص، التوصية، المحافظة على التفاصيل، والتحقق بعد المعالجة**. في المقابل، الواجهة الحالية كبيرة ومتشعبة، وطبقة HTTP تجمع مسؤوليات كثيرة في ملف واحد، كما أن سجل العمليات لا يغطي بصورة مباشرة مجموعة مهمة من تقنيات مقرر معالجة الصور مثل Sobel وLaplacian وCanny والعمليات الهندسية العامة والمرشحات الأساسية.

بناءً على ذلك، القرار الصحيح ليس حذف الفكرة وإنتاج محرر فلاتر عام، بل **إعادة صياغة الفكرة داخل محرر معالجة صور أكاديمي واضح**: يحتفظ بمؤشرات التشخيص وPreservation Verification، ويضيف العمليات التعليمية الناقصة، ويفصل المعالجة عن Flask والواجهة، ويجعل كل عملية قابلة للتجربة والشرح والاختبار.

## جرد المشروع

| العنصر | النتيجة |
|---|---:|
| ملفات المشروع بعد فك الأرشيف | 252 ملفاً تقريباً، تتضمن ملفات تشغيل واختبارات ووثائق وذاكرة مؤقتة |
| وحدات المعالجة الأساسية | 13 وحدة Python داخل `processing/` |
| حجم وحدات المعالجة | نحو 6274 سطراً |
| حجم `app.py` | أكثر من 1100 سطر |
| حجم `operations.py` | نحو 799 سطراً |
| حجم واجهة HTML | نحو 509 أسطر |
| حجم JavaScript | نحو 2118 سطراً |
| حجم CSS | نحو 3054 سطراً |
| ملفات الاختبارات | 15 ملفاً تقريباً، بنحو 6776 سطراً |
| الاعتماديات الرئيسية | Flask، OpenCV، NumPy، pytest |

يوجد داخل الأرشيف عدد من الملفات التي لا ينبغي نقلها إلى النسخة النهائية مثل `.pytest_cache` وملفات `__pycache__` وملفات التشغيل الناتجة. كما أن مجلدات `storage/` مخصصة لملفات runtime ولا ينبغي اعتبار محتواها جزءاً من مصدر المشروع.

## ما سيتم الاحتفاظ بمفاهيمه وإعادة صياغته

| المكوّن الحالي | القرار | سبب القرار |
|---|---|---|
| `processing/analyzer.py` | الاحتفاظ بالمفاهيم وإعادة تنظيمها | يحتوي مؤشرات قابلة للتفسير للسطوع والتباين والحدة والضوضاء وتفاوت الإضاءة وكثافة الحواف، وهي مفيدة أكاديمياً |
| `processing/recommender.py` | الاحتفاظ بمنطق القواعد مع تبسيط العقدة | يقدم توصيات Rule-based قابلة للشرح بدلاً من قرارات غامضة |
| `processing/preservation.py` | الاحتفاظ وإعادة تسمية بعض الواجهات | يمثل الجانب المميز للمشروع: فحص أثر المعالجة على البنية وعدم اعتبار كل زيادة في الوضوح نجاحاً |
| `processing/pipeline.py` | الاحتفاظ بالفلسفة لا بالتعقيد الحالي | فكرة Benefit Gate وPreservation Gate وRollback جيدة، لكن الحد الصارم لخطوة واحدة يحتاج فصلًا عن محرر العمليات اليدوي |
| `processing/preparation_pipeline.py` | تحويله إلى ميزة اختيارية | كشف الحدود والتصحيح المنظوري مفيدان للمخطوطات، لكنهما ليسا شرطاً لكل عملية في مقرر معالجة الصور |
| `processing/document_boundary.py` وباقي وحدات التحضير المتخصصة | تأجيل النقل المباشر | منطق كبير ومتخصص، ويُضاف لاحقاً فقط إذا أثبت التدقيق أو العرض الأكاديمي الحاجة إليه |
| الوثائق الحالية | استخلاص المفاهيم وإعادة الكتابة | تحتوي مادة مفيدة، لكنها موزعة على ملفات كثيرة وتحتاج توحيداً حول بنية المشروع الجديدة |
| الاختبارات الحالية | نقل الحالات المهمة وإعادة كتابة الاختبارات | تغطيتها غنية، لكن عقود API الحالية معقدة ومتداخلة وتحتاج اختبارات أبسط وأكثر مباشرة |

## المشكلات التقنية المرصودة

### 1. تضخم نقطة التشغيل
`app.py` يتولى إنشاء التطبيق، التحقق من الملفات، فك الصور وترميزها، إدارة المجلدات، حفظ manifests، تحليل الصور، تنفيذ العمليات، المعاينات، الـpipeline، التحضير، والاستجابات. هذا يجعل أي تعديل في عملية واحدة معرضاً للتأثير في مسارات غير مرتبطة بها. ستنقل هذه المسؤوليات إلى `routes.py` و`validators.py` و`services/` و`processing/` وفق الخطة.

### 2. العمليات الحالية لا تغطي كامل النطاق المطلوب
السجل الحالي يحتوي على CLAHE، موازنة المدرج، إزالة ضوضاء، sharpening، threshold عالمي وOtsu وadaptive، morphology، تطبيع الإضاءة، gamma، intensity adjustment، قمع الخلفية، وdeskew. لكنه لا يقدم كعمليات مستقلة واضحة قصاً عاماً، تغيير حجم، تدويراً عاماً، انعكاساً، Box Blur، Gaussian Blur، Sobel، Scharr، Laplacian، Canny، كل صيغ threshold الأساسية، أو واجهة منظمة للقنوات وRGB/HSV.

وجود `cv2.Laplacian` و`cv2.Canny` في وحدات التحليل أو التحقق لا يعني أن الطالب يستطيع تشغيلهما كعمليتين واضحتين من الواجهة؛ لذلك ستضاف كعمليات تعليمية مستقلة مع قيم ومعاينة وشرح.

### 3. سجل العمليات غير موحد تماماً
بعض عناصر `OPERATIONS` تملك `risk` و`automatic` و`default_parameters`، بينما عناصر أقدم تكتفي بالاسم والفئة والوصف. كما أن بعض الأوصاف بالإنجليزية وبعضها بالعربية. ستُبنى registry موحدة لكل عملية تحتوي الاسم العربي، الاسم التقني، الفئة، الوصف، المعاملات، الحدود، القيمة الافتراضية، مستوى الكلفة، وهل المعاينة المحلية ممكنة.

### 4. الواجهة الحالية متقدمة بصرياً لكنها مركبة أكثر من اللازم
واجهة `index.html` تجمع شريط سير عمل، رفع، تشخيصاً، رسوم Chart.js، مسارين يدوي وذكي، تحضير وثيقة، مقارنة، تفاصيل تقنية، تاريخاً، وتصديراً. هذا يعطي فكرة غنية لكنه لا يطابق مباشرة التخطيط المطلوب: **شريط جانبي + معاينة مركزية + لوحة خصائص**. كما توجد معرفات HTML مكررة، منها `manualApprovalButton` و`manualChainList` و`manualChainStatus` و`manualManualDownloadButton`، وهي مشكلة فعلية قد تجعل `getElementById` يتعامل مع أول عنصر فقط ويؤدي إلى سلوك غير متوقع.

ستعاد كتابة الواجهة بهيكل أصغر، مع فصل العمليات إلى فئات، وإظهار الخصائص الخاصة بالعملية النشطة فقط، مع الاحتفاظ بالتشخيص والتحقق كلوحات جانبية أو تبويبات مساندة.

### 5. JavaScript يتولى حالات كثيرة داخل ملف واحد
`main.js` يتجاوز 2000 سطر ويجمع الحالة، عناصر DOM، تعريف المعاملات، الرسوم، الرفع، المعاينة، التصدير، التاريخ، المسارات الذكية، والتحقق. ستقسم هذه الأجزاء إلى وحدات مستقلة مع store صغير واضح، وطبقة API، ومحرك Canvas، ووحدة history، ووحدة controls، ووحدة UI.

### 6. المعاينة الحالية تعتمد كثيراً على الخادم
الواجهة الحالية تستدعي مسار المعاينة على Flask للعمليات اليدوية. هذا صحيح للعمليات الثقيلة، لكنه لا يحقق أفضل استجابة للعمليات الخفيفة. ستُنفذ العمليات الهندسية وبعض تحويلات الشدة والرمادي محلياً عند الإمكان، بينما تبقى OpenCV مرجع التنفيذ النهائي والعمليات الثقيلة. سيُحسم التعارض بين المعاينة السريعة ونتيجة OpenCV النهائية في طبقة الحالة، مع إلغاء الطلبات القديمة وdebounce.

### 7. صيغ الإدخال والإخراج تحتاج توسيعاً
الرفع الحالي يقبل JPG وJPEG وPNG، والحفظ الداخلي يعتمد PNG. الإصدار الجديد سيقبل الإدخال المدعوم فعلياً بعد التحقق من OpenCV، ويجعل التصدير صريحاً لـPNG وJPG وWebP مع جودة قابلة للتحكم عندما تكون الصيغة داعمة لها.

### 8. العمليات الهندسية تحتاج نموذجاً واضحاً
العملية الحالية `deskew` موجودة، لكن القص العام والتحويلات الهندسية ليست ضمن registry الأساسية، مع وجود `crop` في JavaScript فقط و`document_prepare` كعنصر واجهة خاص. ستوحد العمليات الهندسية في طبقة مستقلة، وتفصل القص اليدوي عن التحضير التلقائي المتخصص.

## قرار إعادة البناء
ستُعاد كتابة المشروع في نسخة عمل جديدة، مع عدم تعديل الأرشيف الأصلي. سيُنقل الآتي بصورة انتقائية:

1. المؤشرات القابلة للتفسير من analyzer.
2. التوصيات والقواعد المحافظة من recommender بعد توحيد العقدة.
3. فحوص Preservation الأساسية مع جعلها خدمة اختيارية للمعاينة وواجبة عند الاعتماد النهائي.
4. العمليات الحالية التي اجتازت المراجعة، مع إصلاح التحقق والأنواع والقيم.
5. حالات الاختبار التي تمثل سلوكاً حقيقياً، لا تفاصيل implementation قديمة.

وسيُستبدل الآتي:

1. `app.py` الأحادي بواجهات ومسارات صغيرة.
2. `operations.py` الكبير بسجل عمليات ووحدات فئوية.
3. `index.html` المركب بواجهة workspace ثلاثية الأجزاء.
4. `main.js` الواحد بوحدات JavaScript منفصلة.
5. الاعتماد الزائد على Bootstrap وChart.js في الهيكل الأساسي بــCSS مخصص، مع إبقاء أي مكتبة خارجية فقط إذا كانت لها قيمة واضحة.

## نطاق العمليات الجديدة
ستشمل النسخة الجديدة عمليات المقرر الأساسية إلى جانب العمليات المميزة للمخطوطات:

| المجموعة | الحد الأدنى في النسخة الجديدة |
|---|---|
| هندسية | crop، resize، rotate، flip، deskew |
| شدة وإضاءة | grayscale، brightness/contrast، gamma، normalization |
| فلاتر | box، Gaussian، median، bilateral، sharpen |
| حواف | Sobel، Scharr عند الحاجة، Laplacian، Canny |
| Threshold | binary، inverse، trunc، to-zero، adaptive، Otsu |
| Morphology | erosion، dilation، opening، closing، gradient، top-hat، black-hat |
| Histogram | histogram/equalization، CLAHE |
| ألوان | RGB/BGR، HSV، split/merge للقنوات |
| تحليل | contours ومؤشرات أساسية عند الحاجة |
| مخطوطات | illumination normalization، faded text، background suppression، preservation verification |

## مخاطر التوافق
قد تكشف الاختبارات أن بعض السلوك الحالي يعتمد على أسماء manifests أو معرفات نتائج أو شروط خاصة بمسار preparation. لذلك لن يتم نسخ المعرفات أو الملفات الناتجة إلى النسخة الجديدة، بل سيعاد تعريف عقدة بيانات أبسط: صورة أصلية، سلسلة عمليات، نتيجة معاينة، ونتيجة تصدير.

كما أن بعض الدوال الحالية مصممة لقبول صور BGR وBGRA وgrayscale بطرق مختلفة. ستوحد النسخة الجديدة سياسة القنوات في `image_codec.py`، مع اختبارات صريحة حتى لا تتحول معالجة صورة رمادية إلى نتيجة ملونة أو تفقد قناة alpha دون قرار.

## الخلاصة
المشروع الحالي يقدم مادة أكاديمية قوية، لكنه يحتاج إعادة بناء حقيقية في التنظيم وتجربة الاستخدام وتغطية العمليات. أفضل استراتيجية هي الاحتفاظ بالفكرة المميزة **Diagnose → Treat → Preserve → Verify**، ثم وضعها داخل محرر صور تعليمي يطبق عمليات المقرر بوضوح. بهذه الطريقة لا يصبح الناتج نسخة عامة من محرر فلاتر، ولا يضيع العمل البحثي الموجود في المشروع الأصلي.

## ملحق الحالة الحالية — أغسطس 2026

يوثق هذا الملحق ما تحقق بعد مرحلة التحليل السابقة، حتى لا تختلط الخطة التاريخية بالتنفيذ الفعلي.

| الموضوع | الحالة الحالية |
| --- | --- |
| بنية المعالجة | ما زال `app.py` نقطة Flask الرئيسية، بينما أصبحت عمليات OpenCV موزعة في `processing/ops/` ومسجلة عبر `registry.py`. لم تُضف بنية Docker أو Queue أو threading. |
| الواجهة | HTML/CSS/JavaScript مقسمة إلى أجزاء فعلية، مع واجهة عربية RTL ومساحة عمل موحدة للمعاينة والاعتماد والمقارنة. |
| العمليات الهندسية | أضيفت `crop` و`rotate_*` و`flip_*` إلى مسار المعالجة اليدوية؛ لا ينبغي قراءة قائمة العمليات المقترحة القديمة كقائمة وظائف غير موجودة دون الرجوع إلى `registry.py`. |
| المعاينة | العمليات الخفيفة تستخدم Canvas محلياً للعرض، بينما يبقى Flask/OpenCV مصدر artifact عند الاعتماد. |
| السلسلة | كل خطوة لاحقة تستخدم النتيجة المعتمدة السابقة عبر `source_result_id`، مع Undo/Redo لتغيير الحالة المعروضة دون إعادة تنفيذ غير لازم. |
| Smart Preparation | سياسة قبول محافظة؛ C05 قبلت deskew-only بثقة عالية، وC06 بقيت deferred/rejected عند الثقة المنخفضة. |
| Super Resolution | عملية يدوية مستقلة بتكبير Lanczos وUnsharp على luminance، وليست DNN ولا تدعي استعادة معلومات فُقدت. |
| التحقق | النتيجة الحالية هي `333 passed, 16 skipped`، مع نجاح فحص صياغة JavaScript في `static/js/parts/`. |

لذلك، تبقى قيمة هذا التقرير في **تحليل المخاطر وشرح دوافع إعادة البناء**، بينما تُقرأ تفاصيل التنفيذ الحالية من الملفات المرجعية الأحدث. لا يجوز استخدام قسم «نطاق العمليات الجديدة» لإثبات أن كل عنصر مقترح فيه أصبح منفذاً؛ الإثبات الصحيح هو وجود العملية في `registry.py` واختبارها أو توثيقها في تقرير QA.
